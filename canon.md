# canon.md — 正典の階層 v0.1

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-08-26 |
| 位置づけ | このリポジトリ（および実験対象の資料群）における「何が信頼できる唯一の情報源（正典）か」の宣言。すべての文書はこの階層に従う |
| 根拠 | 先行調査 §1（SSOT）・§6（ADR）・§7（IaC 正典化）（research/2026-08-25_先行調査_AI駆動開発時代のドキュメント.md） |

## 原則

**情報1種類につき正典は1つ。正典以外の場所には値・決定・意図を再記述せず、正典への参照だけを書く。** 同じ情報が2箇所に書かれた瞬間、それは矛盾の種になる（[SSOT の定義](https://en.wikipedia.org/wiki/Single_source_of_truth): すべてのデータ要素を一箇所でだけ編集し、他はすべて参照にする）。

## 4つの階層

| 情報の種類 | 正典 | 置き場所（本リポジトリ） | 他の文書がすべきこと |
|---|---|---|---|
| **事実**（設定値・構成・リソース定義） | IaC コード・実データ | `sample-project/` の CFn テンプレート等 | 値を複製せず参照する。「タイムアウトは `template.yaml` の `Resources.ApiFunction` を参照」のように書く |
| **決定**（なぜこの構成にしたか） | ADR（status 付き・追記型） | `decisions/`（M3 周4 で導入。それまでは各文書の決定記述が暫定正典） | 理由を再記述せず ADR 番号で参照する |
| **意図**（何を作るか・受入条件） | spec | `specs/`（導入時期は M3 で判断） | 要求を言い換えず spec の節番号で参照する |
| **議事録・チャット** | **正典ではない（ソース）** | リポジトリに置かない | 直接参照禁止。決定は ADR に起こし、議事録は ADR の出典欄からリンクされるだけの存在にする |

## 正典違反の例（これを検出対象にする）

- 設計書にユーザー数などの揮発的な実値を書く → 設計判断に効く**想定レンジ + 現在値の参照先**を書く
- 設計書が議事録を直接参照する → ADR に起こしてから ADR を参照する
- 同じ決定理由が2つの文書に書かれている → 片方を参照に置き換える
- IaC で表現できる設定値の一覧表を手書きする → パラメータ一覧は生成物とし、手書き設計書には判断の層だけを残す（先行調査 §7）

## ライフサイクル

- **未決事項**は本文に散らさず、文書末尾の Open Questions（またはプロジェクト共通の decision log）に集約する。本文には「（→ Open Questions #n）」の参照だけを置く
- 未決が確定したら ADR を起こし、Open Questions から消して本文の参照を ADR に差し替える（一方向のライフサイクル）
- 決定の変更は ADR を上書きせず、新しい ADR で置き換えて旧 ADR を superseded にする

## enforcement（規約は機構が守らせる）

現状の機構: なし（この規約はまだ「願望」段階）。M3 周4 で adr rules + `/minutes-to-adr` skill、周5 で consistency-checker subagent による正典違反検出を導入し、機構化を完了する（Claude Code 実装レイヤー設計 v0.1、research/2026-08-25_設計_ClaudeCode実装レイヤー.md 参照）。
